Step 3: FileBus is wired to DisplayAdapter in the browser, and a bus-level JSONL export
button 'Download bus JSONL' was added. This downloads BusMessage objects
(direction, timestamp, messageType, payload) exactly as Claude's FileBus spec.

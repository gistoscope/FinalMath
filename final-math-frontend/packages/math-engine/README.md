# math-engine

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build math-engine` to build the library.

## Running unit tests

Run `nx test math-engine` to execute the unit tests via [Vitest](https://vitest.dev/).

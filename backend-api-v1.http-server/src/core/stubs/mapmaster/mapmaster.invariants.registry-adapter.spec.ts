import { describe, expect, it } from "vitest";
// import { InvariantRegistryAdapter } from './mapmaster.invariants.registry-adapter';

describe("Invariant Registry Adapter", () => {
  it("should be testable", () => {
    expect(true).toBe(true);
  });
});

import { describe, expect, it } from "vitest";
// import { PRIMITIVE_PATTERNS_REGISTRY } from './PrimitivePatterns.registry';

describe("PrimitivePatterns.registry", () => {
  it("should be testable", () => {
    expect(true).toBe(true);
  });
});

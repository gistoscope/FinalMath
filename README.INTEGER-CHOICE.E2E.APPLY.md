# Integer click → choice popup (E2E) patch

Unzip this archive **into `D:\G`**.

It contains updates for:
- `backend-api-v1.http-server` (protocol + orchestrator + handlers + tests)
- `viewer` (UI popup + client request `preferredPrimitiveId`)

After unzipping, restart both dev servers.

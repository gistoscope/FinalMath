import { describe, expect, it } from "vitest";
// import { InvariantCourseLoader } from './invariants.course-loader';

describe("Invariants Course Loader (Legacy)", () => {
  it("should be testable", () => {
    expect(true).toBe(true);
  });
});
